{
  "name": "padbol-score-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && tar --exclude='./.git' --exclude='./dist' --exclude='./node_modules' -cf - . | tar -xf - -C dist"
  },
  "dependencies": {},
  "devDependencies": {}
}
